<%= form_tag articles_path, method: :get do %>
  <p>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", name: nil %>
<% end %>


<%= link_to "#{Category.first.name}", home_first_tag_path %>
<ul>
  <% @articles_one.each do |article| %>
    <li><%= link_to "#{article.name}", article_path(article) %></li>
  <% end %>
</ul>

<%= link_to "#{Category.second.name}", home_second_tag_path %>
<ul>
  <% @articles_two.each do |article| %>
    <li><%= link_to "#{article.name}", article_path(article) %></li>
  <% end %>
</ul>
